{
  "name": "inksuite",
  "version": "0.1.0",
  "private": true,
  "description": "Ink Suite — a portfolio of mini utilities for Kraken's Ink L2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "overrides": {
      "axios": ">=1.15.0",
      "follow-redirects": ">=1.16.0",
      "hono": ">=4.12.14",
      "postcss": ">=8.5.10",
      "uuid": ">=14.0.0"
    }
  }
}
